generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Buffer {
  id          String   @id
  owner       String
  publicOwner String
  type        String
  date        DateTime
  content     String
  expiryDate  DateTime
  isPublic    Boolean
}
